# RAG using LlamaIndex

The file app.py should be running in the background for the web api.
it implements that using gradio.

The file main.py implements the CLI.

## Enviroment setup

python3.11 -m venv venv
source venv/bin/activate # activate venv
pip install -r requirements.txt
